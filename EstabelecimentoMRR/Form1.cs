using System.Windows.Forms;

namespace EstabelecimentoMRR
{
    public partial class Form1 : Form
    {
        public Form1()
        {
            InitializeComponent();
        }

        private void Form1_Load(object sender, System.EventArgs e)
        {
            //IDbConnection db = new MySqlConnection(ConfigurationManager.ConnectionStrings["Frota"].ConnectionString);
            //db.Open();
            //try
            //{
            //    var obj = db.Insert<T>(entity);
            //    if (obj > 0)
            //    {
            //        return obj;
            //    }
            //    throw GaException.Exception(GaExceptionError.Insert);
            //}
            //catch (Exception x)
            //{
            //    throw GaException.Exception(x.Message);
            //}
            //finally
            //{
            //    db.Close();
            //}
        }
    }
}
